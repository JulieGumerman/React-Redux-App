const initialState = {
    trails: [],
    isFetching: false,
    error: ""
}

export const trailReducer = (state=initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}